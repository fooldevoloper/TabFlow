const groupToggle = document.getElementById('groupToggle');
const status = document.getElementById('status');
const groupsList = document.getElementById('groupsList');
const settingsIcon = document.getElementById('settingsIcon');
const settingsPanel = document.getElementById('settingsPanel');
const settingsBack = document.getElementById('settingsBack');
const configGroupsList = document.getElementById('configGroupsList');
const modalOverlay = document.getElementById('modalOverlay');
const modalTitle = document.getElementById('modalTitle');
const groupNameInput = document.getElementById('groupName');
const groupEmojiInput = document.getElementById('groupEmoji');
const groupDomainsInput = document.getElementById('groupDomains');
const modalClose = document.getElementById('modalClose');
const modalCancel = document.getElementById('modalCancel');
const modalSave = document.getElementById('modalSave');
const addGroupBtn = document.getElementById('addGroupBtn');
const resetGroupsBtn = document.getElementById('resetGroupsBtn');
const sortNowBtn = document.getElementById('sortNowBtn');
const ungroupBtn = document.getElementById('ungroupBtn');
const conflictModalOverlay = document.getElementById('conflictModalOverlay');
const conflictBody = document.getElementById('conflictBody');
const conflictCancel = document.getElementById('conflictCancel');
const conflictConfirm = document.getElementById('conflictConfirm');
const searchBar = document.getElementById('searchBar');
const searchInput = document.getElementById('searchInput');
const searchResults = document.getElementById('searchResults');
const expandAllBtn = document.getElementById('expandAllBtn');
const autoCollapseToggle = document.getElementById('autoCollapseToggle');
const uiModeToggle = document.getElementById('uiModeToggle');
const duplicatePreventionToggle = document.getElementById('duplicatePreventionToggle');
const groupUnlistedToggle = document.getElementById('groupUnlistedToggle');
const displayModeSelect = document.getElementById('displayModeSelect');

let editingGroupId = null;
let configGroups = [];
let pendingSave = null;
let cachedGroupsData = null;
let searchActive = false;
let focusedGroupId = null;
